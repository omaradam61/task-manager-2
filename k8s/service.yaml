apiVersion: v1
kind: Service
metadata:
  name: task-api-service
spec:
  selector: { app: task-api }
  ports:
    - port: 80
      targetPort: 3000
  type: NodePort        # use LoadBalancer on a real cloud cluster
